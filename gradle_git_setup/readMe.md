Build project with ```./gradlew build```
Run project with ```./gradlew run```

Git commit using:
```git add .```
```git commit -m "message"```
```git push```

Prsumably can push specific things by replacing ```git add .```. With something like:
```git add ./app```

If path not loaded (whatever that means), run ```source ~/.bashrc```